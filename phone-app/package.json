{
  "name": "phone-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Anki-style flashcard app: iOS + backend + web",
  "workspaces": [
    "shared",
    "backend",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace backend && npm run build --workspace web --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md}\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "ci": "npm run format:check && npm run lint && npm run typecheck && npm run build && npm run test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  }
}
